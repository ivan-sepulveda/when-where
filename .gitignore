.DS_Store
# env files (can opt-in for committing if needed)
.env*
!.env.local.example
# Not a secret -- just the public production API host, committed so
# every Vite build (prod or Vercel preview) has it regardless of
# whether Vercel's dashboard env var is set/scoped correctly.
!frontend/.env.production

# python virtual environment (project root)
venv/
__pycache__/
*.pyc

# jupyter
.ipynb_checkpoints/

data/raw/unesco/whc001.json

_to_delete/

data/globalshorelines/*
temp/
Claude outputs/
notebooks/