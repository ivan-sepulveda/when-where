# Copy to .env.local (gitignored) and adjust as needed.
# Base URL of the backend API (see ../backend/README.md).
# Local dev default matches `uvicorn app.main:app --port 8000`.
VITE_API_BASE_URL=http://localhost:8000
