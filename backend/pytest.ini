[pytest]
# Makes `import app.main` etc. resolve from backend/ regardless of what
# directory `pytest` is invoked from -- app/ is a plain package (see
# app/__init__.py), not installed, so it needs to be on sys.path.
pythonpath = .
testpaths = tests
